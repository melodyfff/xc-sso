<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns="http://www.springframework.org/schema/webflow"
      xsi:schemaLocation="http://www.springframework.org/schema/webflow
                          http://www.springframework.org/schema/webflow/spring-webflow.xsd">
    
    <action-state id="initialAuthenticationRequestValidationCheck">
        <evaluate expression="initialAuthenticationRequestValidationAction"/>
        <transition on="authenticationFailure" to="handleAuthenticationFailure"/>
        <transition on="error" to="initializeLoginForm"/>
        <transition on="success" to="ticketGrantingTicketCheck"/>
        <transition on="successWithWarnings" to="ticketGrantingTicketCheck"/>
    </action-state>

    <action-state id="ticketGrantingTicketCheck">
        <evaluate expression="ticketGrantingTicketCheckAction"/>
        <transition on="notExists" to="gatewayRequestCheck"/>
        <transition on="invalid" to="terminateSession"/>
        <transition on="valid" to="hasServiceCheck"/>
    </action-state>
        
    <action-state id="initializeLoginForm">
        <evaluate expression="initializeLoginAction" />
        <transition on="success" to="viewLoginForm"/>
    </action-state>

    <view-state id="viewLoginForm" view="casLoginView" model="credential">
        <binder>
            <binding property="username" required="true"/>
            <binding property="password" required="true"/>
            <!--<binding property="system" required="true"/>-->
        </binder>
        <transition on="submit" bind="true" validate="true" to="checkSubmit" history="invalidate"/>
    </view-state>

    <!--进行判断是否需要检查验证码-->
    <action-state id="checkSubmit" >
        <evaluate expression="initializeLoginCheckCodeAction" />
        <transition on="success" to="realSubmit"/>
        <transition on="needCheck" to="initCheckCode" />
        <transition on="error" to="initializeLoginForm"/>
    </action-state>

    <!--跳转到验证码检查页面-->
    <view-state id="initCheckCode" view="checkCode" model="credential">
        <binder>
            <binding property="code" required="true"/>
        </binder>
        <transition on="submit" bind="true" validate="true" to="doCheckCode" history="invalidate"/>
    </view-state>

    <!--进行验证码检查-->
    <action-state id="doCheckCode" >
        <evaluate expression="checkCodeAction" />
        <transition on="success" to="realSubmit"/>
        <transition on="error" to="initCheckCode"/>
    </action-state>

    <action-state id="realSubmit">
        <evaluate expression="authenticationViaFormAction"/>
        <transition on="warn" to="warn"/>
        <transition on="success" to="sendTicketGrantingTicket"/>
        <transition on="successWithWarnings" to="showAuthenticationWarningMessages"/>
        <transition on="authenticationFailure" to="handleAuthenticationFailure"/>
        <transition on="error" to="initializeLoginForm"/>
    </action-state>
</flow>
